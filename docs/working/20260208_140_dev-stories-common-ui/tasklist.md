# タスクリスト: 開発秘話ページの共通UI化とテーマシステム対応

**ステータス**: 🔄 進行中
**Issue**: #140

## フェーズ1: ページレベルのテーマ対応

### タスク1.1: 一覧ページ（page.tsx）の共通UI化
- [ ] `style={{ backgroundColor: '#F7F7F5' }}` → `bg-page` クラスに置換
- [ ] 戻るリンク（独自Link+HiArrowLeft） → `<BackLink>` に置換
- [ ] `text-gray-800`, `text-gray-500` 等 → `text-ink`, `text-ink-sub` に置換
- [ ] 空状態表示 → CSS変数対応（bg-amber-100等のハードコード色）
- [ ] `transition-colors duration-1000` 追加

### タスク1.2: 詳細ページ（EpisodeDetailClient.tsx）の共通UI化
- [ ] `style={{ backgroundColor: '#F7F7F5' }}` → `bg-page` クラスに置換
- [ ] ヘッダーの戻るリンク → `<BackLink>` に置換
- [ ] ヘッダー背景 `bg-white/50` → `bg-surface/50` に置換
- [ ] `border-gray-200/50` → `border-edge/50` に置換
- [ ] テキスト色 → CSS変数に置換
- [ ] 前後ナビゲーションボタン → `<IconButton>` または `<Button>` に置換
- [ ] エピソード未発見時の表示 → CSS変数対応
- [ ] `transition-colors duration-1000` 追加

## フェーズ2: コンポーネントレベルのテーマ対応

### タスク2.1: EpisodeCard の共通UI化
- [ ] 外枠 `bg-white rounded-xl shadow-sm border border-gray-100` → `<Card>` + CSS変数
- [ ] サムネイル背景 `from-amber-100 to-orange-50` → テーマ対応
- [ ] テキスト色 → CSS変数
- [ ] タグ表示 `bg-gray-100 text-gray-600` → `<Badge>` に置換

### タスク2.2: DetailSection の共通UI化
- [ ] `bg-white rounded-2xl shadow-sm border border-gray-100` → `<Card>` + CSS変数
- [ ] タグ表示 → `<Badge>` に置換
- [ ] `border-t border-gray-100` → `border-edge` に置換

### タスク2.3: DialogueSection のテーマ対応
- [ ] `bg-gradient-to-b from-amber-50 to-orange-50/50` → テーマ対応グラデーション
- [ ] キャラクター紹介カード `bg-white/60` → `bg-surface/60`
- [ ] 関係性説明 `bg-white/40` → `bg-surface/40`
- [ ] テキスト色 → CSS変数

### タスク2.4: DialogueBubble・CharacterAvatar
- [ ] DialogueBubble: キャラ固有色は維持（テーマ変更不要）
- [ ] CharacterAvatar: 変更不要を確認

## フェーズ3: 検証
- [ ] lint 通過
- [ ] build 通過
- [ ] test 通過
- [ ] 通常モード動作確認
- [ ] クリスマスモード動作確認

## 見積もり
- 実装: 約15分（AI実行）
- 検証: 約5分
